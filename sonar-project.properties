# ESLint rule configuration files are declarative — structural repetition
# across modules is inherent to the domain and not a code quality concern.
# SonarCloud CPD tokenizes string literals identically, causing blocks of
# 'plugin/rule': ['error'] entries to be flagged as duplicates across files.
sonar.cpd.exclusions=src/*/rules.ts,src/*/index.ts
