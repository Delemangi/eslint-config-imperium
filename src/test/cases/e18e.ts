const arr = [3, 1, 2];

const sorted = [...arr].sort();

const reversed = [...arr].reverse();

const filled = Array.from({ length: 3 }).map(() => 0);

setTimeout(() => {});

export { filled, reversed, sorted };
